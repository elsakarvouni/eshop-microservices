User Service (Port: 3001)
Αναλαμβάνει μόνο τους χρήστες.

POST /register: Θα δέχεται email και password, θα τα αποθηκεύει στην Postgres (αφού κρυπτογραφήσει το password) και θα επιστρέφει "User created".

POST /login: Θα ελέγχει αν το email/password είναι σωστά και θα επιστρέφει ένα JWT Token (ένα ψηφιακό "πάσο" που αποδεικνύει ποιος είσαι).

Product Service (Port: 3002)
Αναλαμβάνει τον κατάλογο. Δεν ξέρει καν τι πάει να πει "χρήστης".

GET /products: Θα επιστρέφει μια λίστα με όλα τα προϊόντα (όνομα, τιμή, απόθεμα) από τη MongoDB.

POST /products: Θα προσθέτει ένα νέο προϊόν (για τον διαχειριστή).

Order Service (Port: 3003)
Το πιο σημαντικό. Δέχεται παραγγελίες.

POST /orders: Ο χρήστης στέλνει το JWT Token του (για να ξέρουμε ποιος είναι) και το ID του προϊόντος που θέλει να αγοράσει.

Η ροή: Ελέγχει το token -> Καταγράφει την παραγγελία -> Στέλνει μήνυμα στο RabbitMQ: "Μειώστε το απόθεμα αυτού του προϊόντος κατά 1!".